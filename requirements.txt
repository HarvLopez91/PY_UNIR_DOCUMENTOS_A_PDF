# Dependencias de producción para PDF Consolidator
# Core libraries
img2pdf>=0.5.0
pypdf>=3.16.0
pyinstaller>=6.0.0

# Windows Office automation (only on Windows)
pywin32>=306; sys_platform == "win32"

# Standard library dependencies (usually included with Python)
# pathlib - included in Python 3.4+
# tkinter - included with most Python installations
# logging - included in Python standard library
# unicodedata - included in Python standard library